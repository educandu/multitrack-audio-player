import { describe, expect, it } from 'vitest';

describe('index', () => {
  describe('this test has no value', () => {
    it('and should always succeed', () => {
      expect(1).toBe(1);
    });
  });
});
